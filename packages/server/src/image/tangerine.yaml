# Tangerine Lima VM Template
# Golden image for running OpenCode agents in isolated VMs.
# Usage: limactl start --name tangerine-base src/image/tangerine.yaml

# Debian 13 (Trixie) cloud images
images:
  - location: "https://cloud.debian.org/images/cloud/trixie/daily/latest/debian-13-generic-arm64-daily.qcow2"
    arch: "aarch64"
  - location: "https://cloud.debian.org/images/cloud/trixie/daily/latest/debian-13-generic-amd64-daily.qcow2"
    arch: "x86_64"

# Apple Virtualization.framework for better perf on macOS
vmType: "vz"
vmOpts:
  vz:
    rosetta:
      enabled: true
      binfmt: true

cpus: 4
memory: "8GiB"
disk: "20GiB"

ssh:
  localPort: 0
  loadDotSSHPubKeys: true
  forwardAgent: false

# Dedicated non-root user for agent workloads.
# Lima injects SSH keys and grants passwordless sudo automatically.
user:
  name: "agent"
  home: "/home/agent"
  shell: "/bin/bash"

# No host mounts — repos are cloned inside the VM
mounts: []

provision:
  - mode: system
    script: |
      #!/bin/bash
      set -eux -o pipefail
      export DEBIAN_FRONTEND=noninteractive

      # Disable IPv6 — Lima VZ shared networking has broken IPv6 causing ~150x slower downloads
      sysctl -w net.ipv6.conf.all.disable_ipv6=1
      sysctl -w net.ipv6.conf.default.disable_ipv6=1
      echo 'net.ipv6.conf.all.disable_ipv6 = 1' >> /etc/sysctl.d/99-disable-ipv6.conf
      echo 'net.ipv6.conf.default.disable_ipv6 = 1' >> /etc/sysctl.d/99-disable-ipv6.conf

      apt-get update -qq
      apt-get upgrade -y -qq

      apt-get install -y -qq \
        git \
        curl \
        jq \
        openssh-server \
        unzip \
        ca-certificates \
        gnupg

      # Node.js 22 LTS via nodesource
      mkdir -p /etc/apt/keyrings
      curl -fsSL https://deb.nodesource.com/gpgkey/nodesource-repo.gpg.key \
        | gpg --dearmor -o /etc/apt/keyrings/nodesource.gpg
      echo "deb [signed-by=/etc/apt/keyrings/nodesource.gpg] https://deb.nodesource.com/node_22.x nodistro main" \
        > /etc/apt/sources.list.d/nodesource.list
      apt-get update -qq
      apt-get install -y -qq nodejs

      # GitHub CLI (gh)
      mkdir -p -m 755 /etc/apt/keyrings
      curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg \
        | tee /etc/apt/keyrings/githubcli-archive-keyring.gpg > /dev/null
      chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg
      echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" \
        > /etc/apt/sources.list.d/github-cli.list
      apt-get update -qq
      apt-get install -y -qq gh

      # SSH — key-only auth
      sed -i 's/^#\?PasswordAuthentication.*/PasswordAuthentication no/' /etc/ssh/sshd_config

      # Install OpenCode globally
      npm install -g opencode-ai

      # Workspace directory owned by agent user
      mkdir -p /workspace
      chown agent:agent /workspace

      # git: allow operations in any directory
      su - agent -c "git config --global safe.directory '*'"

      # Pre-configure OpenCode for agent user
      su - agent -c "mkdir -p ~/.config/opencode && echo '{}' > ~/.config/opencode/opencode.json"

      # Cleanup
      apt-get clean
      rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*

message: |
  Tangerine VM ready!
  Workspace: /workspace
  User: agent (with passwordless sudo)
