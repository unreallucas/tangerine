{
  "name": "tangerine-server",
  "version": "0.0.8",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "check": "bun x tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@tangerine/shared": "workspace:*",
    "bun-pty": "^0.4.8",
    "effect": "^3.12",
    "hono": "^4.7"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
