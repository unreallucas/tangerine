#!/usr/bin/env bun
// Use bundled CLI when available (npm installs), fall back to source (local dev)
try {
  await import("../dist/cli.js")
} catch {
  await import("../packages/server/src/cli/index.ts")
}
